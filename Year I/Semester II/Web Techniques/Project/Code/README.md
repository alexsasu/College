# Instructions on how to get the website running
1. Name the folder in which you downloaded the files from this folder, "project".  
  
2. To install the "node_modules" folder, run the following command in the terminal: npm install.  
  
3. To start the server, run the following command in the terminal: npm run project.  
